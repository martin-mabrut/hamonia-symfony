{% extends 'base.html.twig' %}

{% block title %}Hello TooController!{% endblock %}

{% block body %}
    {{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.isSuitable) }}
    {{ form_row(form.genres) }}

    <button type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}
